<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Alarm" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_red</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:colorBackground">@color/background_color</item>
        <item name="colorOnBackground">@color/primary_text</item>
        <item name="colorSurface">@color/card_background</item>
        <item name="colorOnSurface">@color/primary_text</item>
        
        <!-- Apply unified styles -->
        <item name="materialButtonStyle">@style/Widget.Alarm.Button</item>
        <item name="materialButtonOutlinedStyle">@style/Widget.Alarm.Button.OutlinedButton</item>
        <item name="materialCardViewStyle">@style/Widget.Alarm.CardView</item>
        <item name="chipStyle">@style/Widget.Alarm.Chip</item>
        <item name="switchStyle">@style/Widget.Alarm.Switch</item>
        <item name="toolbarStyle">@style/Widget.Alarm.Toolbar</item>
    </style>

    <style name="Theme.Alarm" parent="Base.Theme.Alarm" />
    
    <!-- No Action Bar theme -->
    <style name="Theme.Alarm.NoActionBar" parent="Theme.Alarm">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    
    <!-- Full screen theme for alarm screen -->
    <style name="Theme.Alarm.FullScreen" parent="Theme.Alarm">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowBackground">@drawable/gradient_background</item>
    </style>
</resources>